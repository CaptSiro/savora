<?php /** @var Home $this */

use components\core\Html\Html;
use components\core\PageMenu\Footer;
use components\core\PageMenu\Header;
use components\Home\Home;
use core\App;
use core\sideloader\importers\Css\Css;

Css::import($this->getResource('home.css'));

$language = App::getInstance()->getRequest()->getLanguage();
$categories = $this->getCategoryPage();

$projectLink = $this->getProjectLink();
$projectName = $this->getProjectName();

?><?= Header::default() ?>

<main class="home page-container">
    <section class="hero">
        <div class="container">
            <div class="hero-content">
                <h1>Structured Cooking. Smarter Recipes.</h1>
                <p>
                    Discover recipes designed for clarity, consistency, and precision.
                    Built for humans — optimized for systems.
                </p>

                <?php if (!is_null($categories)): ?>
                    <div class="hero-actions">
                        <a href="<?= $categories->getUrl($language) ?>" class="btn primary">Browse Recipes</a>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </section>

    <section class="features">
        <div class="container grid-3">
            <div class="card">
                <h3>Structured Content</h3>
                <p>Recipes follow a strict format for consistent results and easy parsing.</p>
            </div>
            <div class="card">
                <h3>Fast Discovery</h3>
                <p>Find what you need quickly with clear categories and predictable layouts.</p>
            </div>
            <div class="card">
                <h3>AI-Ready</h3>
                <p>Content is designed to integrate seamlessly with intelligent systems.</p>
            </div>
        </div>
    </section>

    <?php if (!is_null($categories)): ?>
        <section class="categories">
            <div class="container">
                <h2>Categories</h2>
                <div class="grid-4">
                    <?php foreach ($categories->getChildren() as $category): ?>
                        <a href="<?= $category->getUrl($language) ?>">
                            <div class="category">
                                <?= Html::escape($category->getLocalizationOrDefault($language)->title) ?>
                            </div>
                        </a>
                    <?php endforeach; ?>
                </div>
            </div>
        </section>
    <?php endif; ?>

    <section class="featured">
        <div class="container">
            <h2>Featured Recipes</h2>
            <div class="grid-3">
                <?php foreach ($this->getFeaturedPages() as $page): ?>
                    <?php $localization = $page->getLocalizationOrDefault($language) ?>
                    <a href="<?= $page->getUrl($language) ?>">
                        <div class="recipe-card">
                            <h4>
                                <?= Html::escape($localization->title) ?>
                            </h4>

                            <?php if (!is_null($meta = $localization->getMeta())): ?>
                                <p><?= Html::escape($meta->description) ?></p>
                            <?php endif; ?>
                        </div>
                    </a>
                <?php endforeach; ?>
            </div>
        </div>
    </section>
</main>

<?= Footer::default() ?>
